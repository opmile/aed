# 101 Review = Arrays

1. Implemente a função `strlen` com ponteiros e sem usar bibliotecas padrão

2. Verifique se uma string é palíndromo

3. Rotacione um array 

    **a)** `k` casas para a direita (clockwise)

    **b)** `k` casas para a esquerda (counter-clockwise)

4. Dado uma string, remova todas as vogais

5. Remova os elementos duplicados em um array

    **a)** Implemente com array auxiliar

    **b)** Implemente com solução *in-place*

6. Dado um array com elementos nulos, mova todos os `0`s para o final do array

7. Dado um array de inteiros com elementos repetidos, conte a frequência de cada elemento

    **i)** Implemente uma solução que retorna uma matriz

    **ii)** A matriz deve conter apenas dois elementos principais:

        1. O primeiro elemento que indica o valor do elemento único 

        2. O segundo que indica sua frequência no array